# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Docker: postgresql://splitshare:splitshare_dev_password@localhost:5432/splitshare
# Production: Use your hosted PostgreSQL connection string
DATABASE_URL=postgresql://splitshare:splitshare_dev_password@localhost:5432/splitshare

# ============================================
# AUTHENTICATION (Better Auth)
# ============================================
# Generate a secure secret: openssl rand -base64 32
BETTER_AUTH_SECRET=generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:5173
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:5173

# ============================================
# RATE LIMITING (Redis via Upstash SDK)
# ============================================
# Docker: Uses upstash-proxy service for Upstash SDK compatibility
# The proxy translates Upstash Redis HTTP protocol to standard Redis commands
# Production: Use Upstash Redis cloud service
UPSTASH_REDIS_REST_URL=http://localhost:8079
UPSTASH_REDIS_REST_TOKEN=example_token

# ============================================
# EMAIL CONFIGURATION
# ============================================
# Docker: Uses Mailpit for email testing (no real emails sent)
# View emails at http://localhost:8025
# Production: Use Resend email service
RESEND_API_KEY=not-needed-for-mailpit
EMAIL_FROM=noreply@splitshare.local

# Production Resend Configuration:
# RESEND_API_KEY=re_your_api_key
# EMAIL_FROM=noreply@yourdomain.com

# ============================================
# ERROR MONITORING (Sentry)
# ============================================
# Optional: Leave empty to disable Sentry in development
# For production, sign up at https://sentry.io and get your DSN
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# ============================================
# APPLICATION CONFIGURATION
# ============================================
PUBLIC_APP_URL=http://localhost:5173
NODE_ENV=development

# Mobile App API Configuration
# For local testing: http://localhost:5173 or http://10.0.2.2:5173 (Android emulator)
# For production: https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:5173

# ============================================
# DOCKER SERVICES REFERENCE
# ============================================
# Start services: docker compose up -d
# View logs: docker compose logs -f [service-name]
# Stop services: docker compose down
#
# Available Services:
# - PostgreSQL: localhost:5432
#   Database: splitshare
#   User: splitshare
#   Password: splitshare_dev_password
#   pgAdmin: http://localhost:5050 (--profile tools)
#   Email: admin@admin.com, Password: admin
#
# - Redis: localhost:6379
#   Password: redis_dev_password
#   Upstash Proxy: http://localhost:8079
#   Redis Commander: http://localhost:8081 (--profile tools)
#
# - Mailpit (Email): http://localhost:8025
#   SMTP: localhost:1025
#
# Optional service profiles:
#   docker compose --profile tools up -d    # Starts pgAdmin & Redis Commander
